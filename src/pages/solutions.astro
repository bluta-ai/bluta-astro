---
import "../styles/global.css";
---
<!doctype html>
<html lang="en">
<head>
  <title>BLUTA — Solutions</title>
  <meta name="description" content="BLUTA Solutions: MCP + local LLM, Connect, Understand, Automate, Outcomes, Case Study." />
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <meta name="theme-color" content="#0b1020" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Comfortaa:wght@700;800&display=swap" rel="stylesheet">
  <style>
    :root{
      --bg:#0b1020;
      --panel:#121a2e;
      --ink:#e5e7eb;
      --muted:#9aa3b2;
      --line:#1f2a44;
      --brand:#00C3D8;
      --brand2:#a78bfa;
      --nav-h:68px;
    }
    *{box-sizing:border-box}
    html{scroll-behavior:smooth}
    body{margin:0;background:linear-gradient(180deg,#0a0f1f 0%,#0b1020 18%,#0b1020 100%);color:var(--ink);font-family:ui-sans-serif,system-ui,-apple-system,Segoe UI,Roboto,Helvetica,Arial}
    a{color:var(--ink);text-decoration:none}
    .container{max-width:1200px;margin:0 auto;padding:0 20px}

    /* NAV */
    .nav{position:sticky;top:0;z-index:50;height:var(--nav-h);backdrop-filter:saturate(1.2) blur(8px);background:rgba(10,14,28,.7);border-bottom:1px solid var(--line)}
    .nav-inner{display:flex;gap:16px;align-items:center;justify-content:space-between;height:100%}
    .brand{display:flex;gap:10px;align-items:center;font-weight:800;font-family:'Comfortaa', ui-sans-serif, system-ui}
    .wordmark{font-weight:800;font-size:24px;letter-spacing:.2px;text-transform:lowercase}
    .wordmark .blu{color:var(--brand)}
    .wordmark .ta{color:#fff}
    .nav-links a{display:inline-block;padding:10px 12px;border-radius:10px;font-weight:700;color:#dbe3f3}
    .nav-links a:hover{background:rgba(255,255,255,.06)}
    .nav-links a.active{background:rgba(255,255,255,.1)}

    /* LAYOUT */
    section{padding:64px 0}
    .grid{display:grid;gap:20px}
    .col-2{grid-template-columns:1fr 1fr}
    .col-3{grid-template-columns:repeat(3,1fr)}
    @media(max-width:980px){.col-2,.col-3{grid-template-columns:1fr}}
    .card{background:linear-gradient(180deg,rgba(255,255,255,.03),rgba(255,255,255,.015));border:1px solid var(--line);border-radius:16px;box-shadow:0 20px 60px rgba(0,0,0,.25)}
    .p-6{padding:18px}
    .btn{display:inline-flex;align-items:center;gap:8px;padding:12px 16px;border-radius:12px;font-weight:800;border:1px solid var(--line);background:rgba(255,255,255,.04);cursor:pointer}
    .btn.primary{background:linear-gradient(120deg,var(--brand),var(--brand2));color:#0b1020;border-color:transparent}
    .subtitle{color:var(--muted);font-size:18px}
    .h1{font-size:clamp(28px,3.6vw,48px);line-height:1.1;margin:10px 0}
    .h2{font-size:30px;margin:0 0 8px}
    .pill{display:inline-flex;align-items:center;gap:8px;padding:6px 10px;border-radius:999px;background:rgba(96,165,250,.12);color:#c7d2fe;font-weight:800;font-size:12px;border:1px solid var(--line)}
    .muted{color:var(--muted)}
    .image{overflow:hidden;border-radius:16px;border:1px solid var(--line);background:#050814}
    .image img{display:block;width:100%;height:100%;object-fit:cover}
    .specs{width:100%;border-collapse:collapse;font-size:14px;margin-top:8px}
    .specs td{padding:12px;border-bottom:1px solid var(--line)}
    .specs td:first-child{width:220px;font-weight:700;color:#bfd1ff}
    .stat{display:flex;gap:12px;align-items:center;border-radius:16px;overflow:hidden;border:1px solid var(--line);background:linear-gradient(180deg,rgba(255,255,255,.03),rgba(255,255,255,.02));padding:10px}
    .stat .thumb{width:64px;height:64px;flex:0 0 64px;background:#091126;border-radius:12px}
    .footer{border-top:1px solid var(--line);padding:40px 0;color:var(--muted)}

    /* HERO (only used on index) */
    .hero{position:relative;padding:0;min-height:calc(68vh + var(--nav-h));}
    .hero-media{position:absolute;inset:0;overflow:hidden}
    .hero-media img{width:100%;height:100%;object-fit:cover;transform:scale(1.02)}
    .hero-overlay{position:relative;z-index:1;display:flex;align-items:flex-end;min-height:calc(68vh + var(--nav-h));background:linear-gradient(180deg,rgba(5,8,20,.15) 0%, rgba(11,16,32,.55) 40%, rgba(11,16,32,.80) 100%)}
    .hero-overlay .content{padding:64px 0}

    /* Solutions extras */
    .lead{font-size:18px;line-height:1.55;color:var(--muted)}
    .eyebrow{font-size:12px;color:#c7d2fe;letter-spacing:.08em;text-transform:uppercase;font-weight:800;margin-bottom:6px}
    .icon{font-size:28px;margin-bottom:10px;display:inline-block}
  </style>

</head>
<body>
  <!-- NAV (Solutions active) -->
  <div class="nav">
    <div class="container nav-inner">
      <a href="index.html#home" class="brand" aria-label="bluta home">
        <span class="wordmark"><span class="blu">blu</span><span class="ta">ta</span></span>
      </a>
      <div class="nav-links">
        <a href="index.html#home" data-nav="home">Home</a>
        <a href="/solutions" data-nav="solutions" class="active">Solutions</a>
        <a href="/hardware" data-nav="hardware">Hardware</a>
        <a href="/projects" data-nav="projects">Projects</a>
        <a href="#contact" data-nav="contact">Contact</a>
      </div>
    </div>
  </div>

  <!-- SOLUTIONS CONTENT (NO HERO) -->
  <section id="solutions">
    <div class="container">
      <div class="h2">MCP + local LLM: AI that acts like a building operator</div>
      <p class="lead">The Multi‑Control Protocol (MCP) makes AI safe and practical in real buildings. It standardises every system and equips the AI with four safeguards:</p>
      <div class="grid col-2" style="margin-top:20px;gap:24px">
        <div class="card p-6"><span class="icon" aria-hidden="true">🕒</span> <strong>History you can rely on</strong><p class="muted">Every system writes into one timeline, so the AI sees context, not fragments.</p></div>
        <div class="card p-6"><span class="icon" aria-hidden="true">⚙️</span> <strong>Safe functions</strong><p class="muted">AI actions (like adjusting HVAC or generating a report) only run through verified, approved functions.</p></div>
        <div class="card p-6"><span class="icon" aria-hidden="true">🛡️</span> <strong>Clear instructions</strong><p class="muted">Guardrails and role‑based prompts keep the AI focused on specific jobs.</p></div>
        <div class="card p-6"><span class="icon" aria-hidden="true">✅</span> <strong>Proof before action</strong><p class="muted">Every AI suggestion cites its source; every action can require confirmation.</p></div>
      </div>
      <div class="card p-6" style="margin-top:20px">
        <div class="grid" style="gap:14px">
          <div class="grid col-3" style="gap:10px">
            <span class="pill">CCTV</span>
            <span class="pill">HVAC / BMS</span>
            <span class="pill">Lifts</span>
            <span class="pill">Access</span>
            <span class="pill">IAQ</span>
            <span class="pill">Meters</span>
            <span class="pill">IoT</span>
          </div>
          <div class="muted" style="text-align:center">↓</div>
          <div class="card p-6" style="text-align:center">
            <div style="font-weight:900">Multi‑Control Protocol (MCP)</div>
            <div class="muted">Standardise • normalise • secure stream</div>
            <div class="grid col-3" style="gap:8px;margin-top:12px">
              <span class="pill">Historical data</span>
              <span class="pill">Function calling</span>
              <span class="pill">Guardrails</span>
              <span class="pill">Proof & verification</span>
            </div>
          </div>
          <div class="muted" style="text-align:center">↓</div>
          <div class="card p-6" style="text-align:center;background:linear-gradient(120deg,var(--brand),var(--brand2));color:#0b1020">
            <div style="font-weight:900">Local large language model (LLM)</div>
            <div style="opacity:.95">Understands • reasons • decides</div>
          </div>
          <div class="muted" style="text-align:center">↓</div>
          <div class="grid col-2" style="gap:10px">
            <div class="card p-6"><div style="font-weight:900">Optimisation</div><div class="muted">HVAC, lights, cleaning, schedules</div></div>
            <div class="card p-6"><div style="font-weight:900">Natural‑language Q&A</div><div class="muted">Ask status, energy, incidents</div></div>
            <div class="card p-6"><div style="font-weight:900">Reports & compliance</div><div class="muted">ESG, safety, audits</div></div>
            <div class="card p-6"><div style="font-weight:900">Operator chat/voice</div><div class="muted">Hands‑free help</div></div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- CONNECT / UNDERSTAND / AUTOMATE -->
  <section>
    <div class="container grid col-2" style="align-items:center">
      <div>
        <div class="eyebrow">Connect</div>
        <h2 class="h2">Connect all your building systems</h2>
        <p class="lead">BLUTA speaks every language — CCTV, HVAC, lifts, meters, and IoT devices. Once connected, everything runs through MCP so your AI agent sees a single, unified view.</p>
      </div>
      <div class="card" style="min-height:180px;text-align:center">🔌 Systems image placeholder</div>
    </div>
  </section>
  <section>
    <div class="container grid col-2" style="align-items:center">
      <div>
        <div class="eyebrow">Understand</div>
        <h2 class="h2">See the full picture, not just scattered data</h2>
        <p class="lead">The AI turns raw building data into insights: energy spikes, unusual access patterns, equipment wear. With historical context, it doesn’t just tell you what’s happening — it explains why.</p>
      </div>
      <div class="card" style="min-height:180px;text-align:center">📊 Insights image placeholder</div>
    </div>
  </section>
  <section>
    <div class="container grid col-2" style="align-items:center">
      <div>
        <div class="eyebrow">Automate</div>
        <h2 class="h2">From monitoring to action, automatically</h2>
        <p class="lead">BLUTA’s local AI agent doesn’t just raise alerts. It can adjust HVAC, issue work orders, or escalate to your staff — all under your rules and approvals.</p>
      </div>
      <div class="card" style="min-height:180px;text-align:center">⚙️ Automation image placeholder</div>
    </div>
  </section>

  <!-- OUTCOMES -->
  <section>
    <div class="container">
      <div class="h2">Cut costs without cutting quality</div>
      <div class="grid col-2" style="margin-top:20px;gap:20px">
        <div class="card p-6"><span class="icon">👥</span> <strong>Labour efficiency (30–50%)</strong><p class="muted">Reduce repetitive monitoring and reporting tasks.</p></div>
        <div class="card p-6"><span class="icon">⚡</span> <strong>Energy savings (15–20%)</strong><p class="muted">Optimised HVAC, lighting, and schedules.</p></div>
        <div class="card p-6"><span class="icon">💰</span> <strong>Lower operating costs</strong><p class="muted">Fewer incidents missed, less downtime, more proactive maintenance.</p></div>
        <div class="card p-6"><span class="icon">🔒</span> <strong>Privacy‑first</strong><p class="muted">All processing stays local. No cloud lock‑in.</p></div>
      </div>
    </div>
  </section>

  <!-- CASE STUDY -->
  <section>
    <div class="container">
      <div class="grid col-2" style="align-items:center">
        <div>
          <div class="eyebrow">Case study</div>
          <h2 class="h2">From manual checks to AI‑assisted operations</h2>
          <p class="lead">A Grade‑A commercial building in <strong>Central, Hong Kong</strong> deployed BLUTA to unify security, safety, and energy systems. Within months, the facility team reported smoother workflows, safer spaces, and measurable savings.</p>
          <ul class="muted" style="margin:10px 0 0 18px;line-height:1.6">
            <li><strong>Workflow efficiency</strong> — Routine monitoring shifted from manual rounds to sensors and AI alerts, reducing repetitive tasks.</li>
            <li><strong>Tenant safety</strong> — Heatmap sensors highlighted overcrowding and unusual patterns in real time, improving readiness and response.</li>
            <li><strong>Energy savings</strong> — HVAC and lighting adjusted to real‑time human traffic, cutting waste while keeping comfort high.</li>
          </ul>
          <p class="muted" style="margin-top:12px">Result: a safer, more efficient building — and a team freed from constant firefighting.</p>
        </div>
        <div class="card" style="min-height:220px;text-align:center">🏢 Building photo placeholder</div>
      </div>
    </div>
  </section>

  <!-- CONTACT / FOOTER -->
  <section id="contact">
    <div class="container">
      <div class="card p-6" style="background:linear-gradient(135deg,rgba(167,139,250,.15),rgba(0,195,216,.15))">
        <div class="grid col-2" style="align-items:center">
          <div>
            <div class="h2">Turn your building into a smart, self‑managing space</div>
            <p class="muted">Request a demo today and see how BLUTA can reduce costs and improve operations from day one.</p>
          </div>
          <div style="text-align:right"><a class="btn primary" href="mailto:sales@blutech.io?subject=BLUTA demo request">Request a demo</a></div>
        </div>
      </div>
      <div class="footer">
        <div class="grid col-3">
          <div>
            <div class="brand"><span class="wordmark"><span class="blu">blu</span><span class="ta">ta</span></span> <span class="muted" style="margin-left:8px">by Blutech.io</span></div>
            <p class="muted" style="font-size:14px;margin-top:8px">Physical AI for smart buildings. BLUTA unifies data and automates operations to cut labour, OPEX and energy.</p>
          </div>
          <div>
            <div style="font-weight:800">Explore</div>
            <div><a href="/solutions" class="muted">Solutions</a></div>
            <div><a href="index.html#hardware" class="muted">Hardware</a></div>
            <div><a href="index.html#feature-matrix" class="muted">Feature Matrix</a></div>
          </div>
          <div>
            <div style="font-weight:800">Contact</div>
            <div><a href="mailto:sales@blutech.io" class="muted">sales@blutech.io</a></div>
            <div class="muted">© <span id="y"></span> Blutech IoT Limited</div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <script>
    document.getElementById('y').textContent = new Date().getFullYear();
  </script>
</body>
</html>
